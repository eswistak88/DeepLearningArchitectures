The summary notebook won't actually work since I can't hold the entire dataset on Github but the methods of file access and counting should be useful if anyone would like to download the dataset themselves.

*stores Jupyter notebooks, can access other folders for datasets and models